# Copy to .env.local. Both are safe to expose — the publishable key only ever
# grants what row level security allows, which for a signed-out visitor is the
# published catalogue and nothing else.
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxx
